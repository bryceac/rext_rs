name: rext
version: "0.1.0"
author: Bryce Campbell
about: utility to easily change file extensions
args:
  - directory:
    short: d
    long: directory
    help: specify directory to traverse
    takes_value: true
    default_value: "."
  - old_extension:
    help: extension to replace
    required: true
    takes_value: true
  - new_extension:
    help: new extension
    required: true
    takes_value: true
  - recursive:
    short: r
    long: recursive
    help: enables recursive mode
  - hidden:
    short: H
    long: include-hidden
    help: include hidden files
  - verbose:
    short: v
    long: verbose
    help: enable verbose mode